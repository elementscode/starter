## Elements Starter Project

This repo contains the project files automatically generated when you create a
new elements application with `elements create app myapp`.

This code is copied into the elements templates before release. It also serves
as an open example of the layout for an elements application.
